Clone the repository:

```bash
git clone https://github.com/mohamed-faris77/SculpxTechlabs.git

Task A&B :

Backend:
cd backend
npm install
# configure environment variables (DB connection, secrets, etc.)
npm start

Frontend:
cd ../frontend
npm install
# configure environment variables 
npm start
